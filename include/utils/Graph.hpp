#define GRAPH_HPP
#ifndef GRAPH_HPP

#include <list>

class Graph {
protected:
    std::list<std::list>> graph;
public:
    Graph();
    MergeVertex(int, int);
    randomize(int);
    randomize(std::list<int>);
};

#endif // GRAPH_HPP